<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>See Users</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-zinc-900 text-white w-full h-screen p-3">
        <div class="w-1/3 flex items-center justify-between">
            <h1 class="text-4xl italic">All Users</h1>
             <p class="underline text-blue-400 text-lg"><a href="/">Go to HomePage</a></p>
        </div>
        <% if(users.length>0){ %>
        <div class="m-5 gap-5 flex flex-wrap">
                <% users.map(user=>{ %>
                    <div class="card bg-zinc-500 p-4 rounded-md w-1/5 flex flex-col items-center">
                        <div class="rounded-md">
                            <img src=<%= user.userImgUrl  %> alt="userImg" class="aspect-square">
                        </div>
                        <div>
                            <h3 class="text-2xl"><%= user.username %></h3>
                            <p class="text-xl"><%= user.email %></p>
                        </div>
                        <div class="w-full mt-5 flex justify-around items-center">
                            <button class="px-1 rounded-md text-yellow-600 border-2 border-yellow-600 font-bold hover:bg-yellow-600 hover:text-black"><a href="/edit/<%= user._id %>">Edit</a></button>
                            <button class="px-1 rounded-md text-red-700 border-2 border-red-700 font-bold hover:bg-red-700 hover:text-white"><a href="/delete/<%= user._id %>">Delete</a></button>
                        </div>
                    </div>
               <% }) %>
            </div>
        <% }else{ %>
                 <h2 class="text-2xl text-red-300 mt-5">No users</h2>
        <% } %>
    </div>
</body>
</html>